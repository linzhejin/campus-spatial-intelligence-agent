# ===== 漫步珞珈 (WHU-Walker) 生产依赖（精简版） =====
# 只含运行时必需，去掉 osmnx/geopandas 等重型 GIS 依赖
# 路网缓存 data/whu_road_network.graphml 已打包进镜像，运行时无需下载

# Web 框架
flask>=3.0
flask-cors>=4.0

# LLM API
openai>=1.0
httpx>=0.27

# 数据校验
pydantic>=2.0

# 图算法（加载 GraphML + Dijkstra）
networkx>=3.0

# 环境配置
python-dotenv>=1.0

# 生产部署
gunicorn>=22.0
