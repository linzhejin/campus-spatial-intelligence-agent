[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -v --basetemp=./.pytest_tmp
markers =
    e2e: end-to-end tests that require a running Flask server (deselect with '-m "not e2e"')
