<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="online.whuspati.app"
    android:versionCode="6"
    android:versionName="1.4.1">

    <uses-sdk android:minSdkVersion="26" android:targetSdkVersion="34"/>

    <!-- Android 11+ 包可见性：声明后 queryIntentServices 才能枚举到
         厂商/系统的 RecognitionService（华为/讯飞/小米等语音引擎） -->
    <queries>
        <intent>
            <action android:name="android.speech.RecognitionService"/>
        </intent>
    </queries>

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <!-- 应用内更新：拉起系统安装器安装新版 APK（用户仍需在系统弹窗点确认） -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES"/>

    <application
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="false"
        android:allowBackup="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:configChanges="orientation|screenSize|keyboardHidden|screenLayout|smallestScreenSize"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- 应用内更新：把下载到应用私有目录的 APK 临时授权给系统安装器读取 -->
        <provider
            android:name=".ApkFileProvider"
            android:authorities="online.whuspati.app.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true"/>
    </application>
</manifest>
