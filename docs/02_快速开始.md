# 02 · 快速开始

## 环境准备

```bash
git clone <repo-url>
cd campus-spatial-intelligence-agent
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate
pip install -r requirements.txt
```

## 配置环境变量

在项目根目录创建 `.env` 文件（参考 `.env.example`）：

```env
DEEPSEEK_API_KEY=sk-your-deepseek-key
AMAP_KEY=your-amap-js-api-key
AMAP_SECURITY_CODE=your-amap-security-code  # 高德 JS API 2.0 安全密钥
```

> LLM 未配置时，系统会降级为规则兜底解析；高德 Key 缺失会影响地图底图与天气接口。

## 初始化路网

```bash
python scripts/validate/network/validate_osm_network.py
```

首次运行自动下载武大周边 OSM 路网（约 30s），后续从缓存加载（<1s）。

## 启动服务

```bash
python app.py
```

打开浏览器访问 http://localhost:5000。

## 试试这些 Query

- 「从珞珈门到樱顶」
- 「从珞珈门骑车到樱花大道」
- 「我想避开陡坡，从行政楼到枫园」
- 「从总图书馆到凌波门，走风景好的路线」
- 「开车从信息学部到文理学部教五」

## 出行方式

页面顶部固定栏提供 🚶 步行 / 🚴 骑行 / 🚗 驾车 切换按钮，也可在自然语言中直接说"骑车""开车""走路"。切换时自动重算路线。

## 数据校验

```bash
python scripts/validate/validate_all_data.py
pytest
```

应输出 232 passed，零失败。
