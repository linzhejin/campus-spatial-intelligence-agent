# 09 · 发布记录

> 产出阶段: Stage 10 · 发布与验收
> 角色: Release Manager
> 状态: 已发布

## 版本信息

- 版本号: v0.1.0
- 发布日期: 2026-08-26
- 部署平台: 腾讯云（CVM）
- 线上地址: http://152.136.102.172:5000/
- 监听端口: 5000
- 运行模式: production（FLASK_ENV=production）
- 进程模型: gunicorn 2 workers × 4 threads（gthread worker class）

## 发布内容

- 漫步珞珈 · WHU-Walker 首个线上版本
- 自然语言驱动的校园路径规划（最短/风景优先/平坦优先三模式）
- LLM 权重提取 + 多约束路由计算（坡度/景观/长度上限）
- POI 查询与推荐
- Leaflet + 高德地图前端展示
- 健康检查端点 `/health` 与 `/api/health`

## 发布检查清单

- [x] PRD 所有 P0 功能验收通过
- [x] 代码审查通过
- [x] QA 测试通过（2026-08-26 部署后巡检）
- [x] 文档完整
- [x] 部署就绪

## 已知问题

1. **`/api/health` 端点原缺失**：腾讯云负载均衡若按 `/api/health` 探活会拿到 404，已于本次发布修复为与 `/health` 同效别名。
2. **`GET /@vite/client` 请求**：巡检时观察到一次该请求，源代码全量检索未发现 vite 引用，判定为浏览器扩展（如 Vue/React Devtools）注入，非项目问题，无需处理。
3. **HTTP 明文协议**：当前未启用 HTTPS，后续接入域名时建议配置 SSL 证书。

## 回滚方案

- **触发条件**：线上服务 5xx 错误率 > 5%，或核心问答/路径规划功能不可用持续 5 分钟。
- **回滚步骤**：
  1. SSH 登录腾讯云 CVM（152.136.102.172）
  2. 切换到应用目录，`git checkout <上一个稳定 commit>`
  3. 重启 Flask 服务
  4. 验证 `/health` 返回 200
- **责任人**：项目负责人
- **预计耗时**：< 5 分钟

## 巡检证据

部署后巡检（2026-08-26）由浏览器自动化执行，结果汇总：

| # | 项目 | 结果 |
|---|---|---|
| 1 | 首页加载 | PASS — 标题/布局/Leaflet 地图正常 |
| 2 | 交互问答 | PASS — "图书馆在哪里"返回正确 POI 与标注 |
| 3 | API 健康检查 | 修复后 PASS（`/api/health` 别名生效） |
| 4 | 控制台与网络 | PASS — 无 JS 错误，`@vite/client` 判定为扩展干扰 |
| 5 | 移动端响应式 | PASS — 488x756 窄屏布局正常 |
